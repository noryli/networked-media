<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Meowdel Show - Home</title>
    <link rel="stylesheet" href="/css/home.css">

</head>
<body>

    <!-- Hall of Fame Title -->
    <h1 class="hall-of-fame">Hall of Fame</h1>

    <!-- Podium Display -->
    <div class="podium">
        <!-- Second Place -->
        <% if (topCats[1]) { %>
            <div class="podium-box second-place">
                <span class="rank">🥈</span>
                <img src="<%= topCats[1].image %>" class="cat-image">
                <p class="cat-name"><%= topCats[1].name %></p>
            </div>
        <% } %>

        <!-- First Place -->
        <% if (topCats[0]) { %>
            <div class="podium-box first-place">
                <span class="rank">🥇</span>
                <img src="<%= topCats[0].image %>" class="cat-image">
                <p class="cat-name"><%= topCats[0].name %></p>
            </div>
        <% } %>

        <!-- Third Place -->
        <% if (topCats[2]) { %>
            <div class="podium-box third-place">
                <span class="rank">🥉</span>
                <img src="<%= topCats[2].image %>" class="cat-image">
                <p class="cat-name"><%= topCats[2].name %></p>
            </div>
        <% } %>
    </div>

    <!-- Outfit Theme Section -->
    <div class="theme-section">
        Today's Theme: <%= todayTheme %>
    </div>

    <!-- Buttons -->
    <div class="button-container">
        <a href="/upload" class="action-btn">Join the Contest</a>
        <a href="/vote" class="action-btn">Be a Judge</a>
        <a href="/leaderboard" class="action-btn">View Rankings</a>
    </div>

</body>
</html>
